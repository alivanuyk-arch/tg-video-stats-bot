# 📊 Telegram бот для аналитики видео

Конструктор SQL-запросов с обучением, который преобразует вопросы на естественном языке в SQL и выполняет их в реальной базе данных видео-статистики.

## 🎯 Особенности

- **🤖 Умный конструктор SQL**: Rule-based шаблоны + LLM (OLLAMA) для сложных запросов
- **📚 Обучение на лету**: Автоматически учится на ответах LLM и ручных примерах
- **💬 Полный Telegram интерфейс**: Команды `/start`, `/learn`, `/stats`, `/clear`
- **🏎️ Высокая производительность**: PostgreSQL в 10+ раз быстрее SQLite для аналитических запросов
- **🔧 Гибкая архитектура**: Работает как с Docker, так и с локальными сервисами

## 🏗️ Архитектура
Пользователь → Telegram Bot → QueryConstructor → {Кэш → Паттерны → LLM} → PostgreSQL → Ответ

text

### Компоненты:
- **QueryConstructor**: Ядро системы, совмещает rule-based логику с LLM
- **LLMTeacher**: Интеграция с OLLAMA (Mistral:7b-instruct) для генерации SQL
- **DatabaseManager**: Асинхронная работа с PostgreSQL через asyncpg
- **Telegram Bot**: Интерфейс пользователя на aiogram 3.x

## 🚀 Быстрый старт

### Вариант 1: Только БД в Docker (рекомендуется для разработки)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/alivanuyk-arch/tg-video-stats-bot.git
cd tg-video-stats-bot

# 2. Настроить окружение
cp .env.example .env
# Отредактировать .env: добавьте TELEGRAM_TOKEN от @BotFather

# 3. Запустить PostgreSQL в Docker
docker-compose -f docker-compose.simple.yml up -d

# 4. Установить зависимости Python
pip install -r requirements.txt

# 5. Загрузить тестовые данные
python scripts/load_data.py

# 6. Установить OLLAMA и модель (опционально, для LLM)
# Скачать с https://ollama.com
ollama pull mistral:7b-instruct

# 7. Запустить бота
python main.py
Вариант 2: Полностью локально (без Docker)
bash
# Установить PostgreSQL
sudo apt install postgresql

# Создать базу данных и пользователя
sudo -u postgres psql -c "CREATE DATABASE video_analytics;"
sudo -u postgres psql -c "CREATE USER bot_user WITH PASSWORD 'password';"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE video_analytics TO bot_user;"

# Загрузить схему
psql -d video_analytics -U bot_user -f scripts/create_tables.sql

# Дальше шаги 2,4,5,6,7 из варианта 1
📖 Использование
Примеры запросов к боту:
text
Сколько всего видео в системе?
Видео с лайками более 5000
Сумма комментариев за 29 ноября 2025
Сколько всего креаторов?
На сколько просмотров выросли все видео 28 ноября 2025?
Сколько видео у креатора с id abc123?
Видео с просмотрами от 1000 до 5000
Команды бота:
/start — показать справку и примеры

/learn "Вопрос?" | SELECT ... — добавить ручной пример

/stats — статистика конструктора (паттерны, попадания, вызовы LLM)

/clear — очистить кэш запросов

🛠️ Технические детали
Конструктор запросов (QueryConstructor)
Кэш точных запросов: Мгновенный ответ на повторяющиеся запросы

Шаблоны (паттерны): Обобщенные SQL-шаблоны с плейсхолдерами

Автозаполнение: Автоматически извлекает даты, числа, ID из запроса

Обучение: Сохраняет новые паттерны из успешных ответов LLM

Интеграция с LLM
Модель: Mistral:7b-instruct (можно заменить на phi, llama и другие)

Промпт: Специализированный для SQL-генерации с правилами по таблицам

Валидация: Проверка безопасности SQL перед выполнением

Fallback: При ошибке LLM используется rule-based логика

База данных
PostgreSQL 15: Оптимизирована для аналитических запросов

Схема:

videos: Основная статистика по видео (создатель, метрики)

video_snapshots: Почасовые изменения метрик для анализа прироста

Индексы: На creator_id, video_created_at, created_at

📊 Тестирование
bash
# Тест LLM интеграции
python tests/test_llm_stability.py

# Тест конструктора
python tests/test_constructor.py
📁 Структура проекта
text
tg-video-stats-bot/
├── src/                    # Исходный код
│   ├── bot.py             # Telegram бот
│   ├── query_constructor.py # Конструктор SQL с обучением
│   ├── llm_fallback.py    # Интеграция с OLLAMA
│   ├── database.py        # Работа с PostgreSQL
│   └── config.py          # Конфигурация
├── scripts/               # Скрипты инициализации
│   ├── create_tables.sql  # Схема БД
│   └── load_data.py       # Загрузка тестовых данных
├── tests/                 # Тесты
├── Data/                  # Исходные данные (JSON)
├── docker-compose.simple.yml # Конфигурация Docker
├── .env.example           # Шаблон переменных окружения
├── requirements.txt       # Зависимости Python
└── README.md             # Эта документация
🔧 Настройка
Переменные окружения (.env):
ini
# Telegram
TELEGRAM_TOKEN=ваш_токен_бота

# База данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=video_analytics
DB_USER=postgres
DB_PASSWORD=postgres

# OLLAMA (опционально)
ENABLE_LLM=true
OLLAMA_MODEL=mistral:7b-instruct
OLLAMA_BASE_URL=http://localhost:11434

# Логирование
LOG_LEVEL=INFO
LLM Модели
Поддерживаемые модели OLLAMA:

mistral:7b-instruct (рекомендуется)

phi:2.7b (легкая альтернатива)

llama3.2:3b

codellama:7b (специализированная для кода)

🚀 Производительность
PostgreSQL vs SQLite: В 10+ раз быстрее на аналитических запросов с агрегациями

Connection pooling: asyncpg эффективно управляет соединениями

Кэширование: Повторные запросы выполняются мгновенно

Асинхронность: Все операции неблокирующие

📈 Roadmap
Веб-интерфейс для просмотра статистики

Поддержка большего количества LLM провайдеров

Графики и визуализации результатов

Экспорт данных в CSV/Excel

Расширенная аналитика (тренды, прогнозы)

📄 Лицензия
MIT

🤝 Вклад в проект
Форкните репозиторий

Создайте ветку для новой функциональности

Закоммитьте изменения

Отправьте Pull Request

🐛 Поддержка
При возникновении проблем:

Проверьте логи в logs/ директории

Убедитесь, что PostgreSQL и OLLAMA запущены

Проверьте настройки в .env файле

Бот готов к работе! Задавайте вопросы на естественном языке и получайте аналитику по видео-статистике.